name: Gatsby DevOps
on: [push]
jobs:
  Install_Dependencies:
    container: node:lts-alpine3.14
    runs-on: ubuntu-latest
    steps:
      - run: npm install
      - run: npm install -g gatsby-cli
      - run: gatsby build